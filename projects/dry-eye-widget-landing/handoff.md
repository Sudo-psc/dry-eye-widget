# Handoff

## Current State

The landing page V1 is built in `landing/` and verified locally.

## Next Actions

1. Replace SVG carousel previews with real macOS/Windows screenshots.
2. Decide whether the landing stays in this repo or moves to a separate GitHub repository.
3. Provide VPS and DNS access details.
4. Deploy `landing/dist/`.
5. Run production PageSpeed and fix any production-only issues.

## Blockers

- VPS credentials and DNS access are needed for deployment.
- Separate landing GitHub repo name is needed if this should not stay in the existing app repository.
